/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.ctfassets.net'], // Allow images from Contentful domain
    },
  };
  
  export default nextConfig;
  